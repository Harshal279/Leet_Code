#include<iostream>
#include<vector>
#include<string>
using namespace std;
int main() {
    string s = "abccb"; 

    for(int i = 0; i<s.length();i++){
        //for odd
        if (s[i-1] == s[i+1]){

        }
        // for even
        if (s[i] == )
    }
      
    int left = 0;
    int right = 0;
    while (left >= 0 && right < s.length()){
        if (s[left]==s[right]){
            
        }
    }
}